name: Build Mo-Home APK

on:
  workflow_dispatch:
  push:
    paths:
      - 'mobile/**'
      - '.github/workflows/build-apk.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: mobile/package-lock.json

      - name: Setup Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 17

      - name: Install Capacitor
        working-directory: mobile
        run: |
          npm install || { echo "::error::npm install failed"; exit 1; }
          npx cap sync android || { echo "::error::cap sync android failed"; exit 1; }

      - name: Build APK
        working-directory: mobile/android
        run: |
          chmod +x gradlew
          set +e
          ./gradlew assembleDebug > ../gradle.log 2>&1
          code=$?
          if [ $code -ne 0 ]; then
            echo "::error::Gradle build failed (exit $code)"
            tail -n 80 ../gradle.log | grep -aiE "FAILURE|What went wrong|Caused by|Exception|error:|failed|Could not|Unable|no sdk|license|not found" | tail -n 6 | while IFS= read -r line; do echo "::error::$line"; done
            exit $code
          fi

      - name: Rename APK
        run: cp mobile/android/app/build/outputs/apk/debug/app-debug.apk mo-home.apk

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: mo-home-apk
          path: mo-home.apk

      - name: Publish to Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: apk
          name: 默的小屋 APK
          files: mo-home.apk
          update_release: true
          fail_on_unmatched_files: false
